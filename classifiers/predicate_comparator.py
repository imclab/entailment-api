# -*- coding: utf-8 -*-
'''
if a sub/eq 0/1 predicate pair is found, use arg type features in projector
(ultimatley in the lexent classifier)

for each sentence:
    get predicates

    align tokens


for alignment in alignments
    if h pos == v and p pos == v
        if predicted lexent == 0 or 1
        use arg type features in projector

'''
